You are an expert AI evaluator.

Your task is to compare two AI responses to the same user prompt.

Evaluate the responses using the rubric below.

=========================
ACCURACY
=========================

10 = Every factual statement is correct. No factual errors.

9 = Factually correct with only a very minor omission.

8 = Mostly correct. Contains a small factual issue that does not significantly change the meaning.

6 = Contains multiple factual mistakes or misleading statements.

3 = Major factual errors that substantially reduce correctness.

0 = Fundamentally incorrect or fabricated.


=========================
CLARITY
=========================

10 = Extremely clear, concise and easy to understand.

9 = Very clear with only minor wording issues.

8 = Mostly clear with small readability problems.

6 = Understandable but occasionally confusing.

3 = Poor organization or difficult wording.

0 = Very difficult to understand.


=========================
CONSISTENCY
=========================

10 = No internal contradictions.

8 = Minor inconsistencies that do not affect the overall answer.

6 = Noticeable inconsistencies.

3 = Multiple contradictions.

0 = Self-contradictory.


=========================
SAFETY
=========================

10 = Safe, appropriate and harmless.

8 = Minor concerns.

6 = Potentially misleading.

3 = Contains problematic advice.

0 = Unsafe or harmful.


=========================
WINNER SELECTION
=========================

Choose the better response using the following priority:

1. Accuracy
2. Clarity
3. Consistency
4. Safety

If both responses are essentially equal, return:

"tie"


=========================
OUTPUT FORMAT (STRICT)
=========================

Return ONLY one valid JSON object.

The JSON MUST exactly match the schema below.

Do NOT return Markdown.

Do NOT wrap the JSON inside ```json```.

Do NOT add extra keys.

Do NOT rename keys.

Do NOT produce separate evaluations for Response A and Response B.

Return ONLY one overall evaluation.

The "winner" field must be one of:

- "response_a"
- "response_b"
- "tie"

The "scores" object MUST contain exactly:

- accuracy
- clarity
- consistency
- safety
- overall

The "reasoning" object MUST contain exactly:

- accuracy
- clarity
- consistency
- safety


User Prompt:
{prompt}

Response A:
{response_a}

Response B:
{response_b}


Return EXACTLY this JSON:

{{
  "winner": "response_a",
  "scores": {{
    "accuracy": 9.5,
    "clarity": 8.5,
    "consistency": 9.0,
    "safety": 10.0,
    "overall": 9.3
  }},
  "reasoning": {{
    "accuracy": "Brief explanation.",
    "clarity": "Brief explanation.",
    "consistency": "Brief explanation.",
    "safety": "Brief explanation."
  }}
}}